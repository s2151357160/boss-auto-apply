# 更新记录

本项目遵循“先记录、发布时再确定版本号”的方式维护改动。尚未合并或发布的内容统一列在 `Unreleased`。

## Unreleased

### 修复

- 修复投递线程未传入 `allow_negotiable`，导致扫描时持续出现 `name 'allow_negotiable' is not defined` 的问题。
- 修复 ADB 服务冷启动较慢时被过早判定为“未检测到手机”的问题，启动服务后会在限定时间内轮询等待设备。
- 区分 ADB 的已连接、未授权、离线和未检测到设备状态，便于判断 USB 调试授权或连接异常。

### 改进

- 移除对杭州职位的硬编码筛除；新版只按用户填写的排除关键词进行过滤。
- 新增“启动/检测ADB”按钮和后台状态监控，软件启动后会定期刷新 ADB 状态，并仅在状态变化时记录日志。
- README 和软件内使用说明补充 ADB 启动、状态含义、跨电脑授权及常见故障排查方法。
