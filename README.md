---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '6e773440-a17a-460b-8781-a7421ae603e2'
  PropagateID: '6e773440-a17a-460b-8781-a7421ae603e2'
  ReservedCode1: 'fbfd3347-404a-4542-ae39-ec2c978eff60'
  ReservedCode2: 'fbfd3347-404a-4542-ae39-ec2c978eff60'
---

# BOSS直聘自动投递工具

基于 ADB/HDC + OCR 的 BOSS直聘自动投递效率工具，支持安卓和鸿蒙手机，自动识别岗位信息并批量投递。

## 功能特性

- **双平台支持**：安卓(ADB) + 鸿蒙(HDC)，GUI一键切换，自动记住上次选择
- **模拟器支持**：MuMu12/MuMu6/雷电/夜神/逍遥，自动 adb connect，端口自动映射
- **多设备安全**：ADB 自动 `-s 序列号`，HDC 自动 `-t 序列号`，多设备同时连接不冲突
- **关键词过滤**：包含/排除关键词，支持中英文逗号和空格分隔
- **薪资筛选**：设定最低/最高薪资(K)，支持区间重叠匹配
- **面议岗位**：可选投递薪资显示"面议"的岗位
- **自动投递**：检测"立即沟通"自动点击，检测"继续沟通"自动跳过，点击后验证是否生效
- **防重复投递**：同一公司只投递一次，持久化到JSON，重启仍有效
- **黑名单公司**：始终跳过黑名单中的公司，GUI添加/查看/清空
- **投递统计面板**：实时显示投递成功/跳过/重复/总计扫描/用时/成功率
- **暂停/继续**：投递过程中随时暂停，当前步骤完成后暂停不丢进度
- **即时停止**：停止信号立即响应，不会等延迟结束
- **投递完成通知**：非阻塞弹窗(3秒自动关) + 自定义音效(mp3/wav/蜂鸣)
- **投递日志双文件**：成功日志和失败日志分开，中文日期时间戳命名
- **日志实时显示**：识别结果区滚动显示 ✓/✗/↻ 汇总行
- **配置持久化**：所有设置自动保存，下次启动自动恢复
- **异常自动恢复**：截图/OCR/点击/滑动/返回失败自动重试，跳过当前岗位不中断
- **自动防风控**：随机偏移点击、每投递完成后1/5概率随机上滑、每10家休息5-10秒、延迟随机波动
- **分辨率自动适配**：优先读取Override size，适配分辨率覆盖的手机
- **设备自动重连**：检测不到设备时自动 kill-server + start-server 重试
- **线程安全**：子线程不直接访问Tkinter控件，按钮互斥禁用防并发，JSON读写加锁防竞态
- **优雅退出**：关闭窗口时通知工作线程停止，避免资源泄漏

## 技术栈

- **GUI**：tkinter（原生 Python GUI）
- **OCR**：RapidOCR（基于 ONNX Runtime）
- **设备驱动**：ADB（安卓） + HDC（鸿蒙），统一抽象层
- **打包**：Nuitka 全编译（Python → C → 机器码，源码不可逆向）

## 文件说明

| 文件/目录 | 说明 |
|-----------|------|
| `boss_gui.py` | 主程序（GUI + 投递逻辑） |
| `device_driver.py` | 设备驱动抽象层（ADB/HDC统一接口） |
| `boss_ocr.py` | OCR 独立测试模块 |
| `boss.ico` | 应用图标 |
| `platform-tools/` | ADB 工具（adb.exe 及依赖DLL，打包进exe） |
| `hdc/` | HDC 工具（hdc.exe 及依赖DLL，打包进exe） |
| `libs/` | 依赖库（RapidOCR/onnxruntime/numpy/cv2/PIL等，打包进exe） |
| `boss_config.json` | 配置文件（自动生成，保存上次设置） |
| `已投递公司.json` | 已投递公司记录（自动生成，防重复投递） |
| `黑名单公司.json` | 黑名单公司记录（自动生成） |
| `投递日志/` | 投递日志目录（按时间戳命名，成功/失败分开） |

## 使用方法

### exe 运行

直接双击 exe 即可，内含 ADB、HDC、OCR 模型及全部依赖，无需额外安装。

exe 同级目录会自动生成配置文件和日志：
- `boss_config.json` — 配置自动保存和恢复
- `已投递公司.json` — 已投递公司记录
- `黑名单公司.json` — 黑名单公司记录
- `投递日志/` — 每次投递的详细日志

### 源码运行

1. 安装 Python 3.12+
2. 安装依赖：
   ```bash
   pip install rapidocr_onnxruntime[onnxruntime] opencv-python pillow pyclipper shapely
   ```
3. 源码运行时 ADB/HDC 需放在 `platform-tools/` 和 `hdc/` 目录下
4. 手机开启 USB 调试（安卓）或开发者模式（鸿蒙），连接电脑
5. 运行：
   ```bash
   python boss_gui.py
   ```

### 模拟器使用

1. 选择对应模拟器（MuMu12/MuMu6/雷电/夜神/逍遥），程序自动 adb connect
2. 先启动模拟器再点击"开始投递"
3. 端口自动映射：MuMu12=16384, MuMu6=7555, 雷电=5555, 夜神=62001, 逍遥=21503

### 鸿蒙手机

纯血鸿蒙 NEXT 不支持 ADB，需选择"鸿蒙(HDC)"平台，手机开启开发者模式。

## 注意事项

- 需要手机通过 USB 连接电脑，开启 USB 调试/开发者模式（模拟器除外）
- 手机屏幕分辨率工具自动适配（优先 Override size）
- 投递间隔建议不低于 3 秒，避免触发平台风控
- USB 驱动为系统内核级，无法打包进 exe，需用户手动安装
- 投递过程中所有操作按钮和平台下拉框自动禁用，防止误操作

## 自动停止条件

- 达到投递次数目标
- 连续3次识别到同一岗位（列表到底）
- 检测到弹窗关键词（投递上限/操作频繁/账号异常等）
- 手动点击"停止投递"（即时响应）